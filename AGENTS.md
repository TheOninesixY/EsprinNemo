## 工业级代码、日志与 UI 文案工程规范

### 1. 代码注释规范 (Code Comments)
- **严格去人称与去教程化**：严禁出现任何第一/第二人称代词（你/我/我们/您）；严禁在源码中编写部署教程、命令行指引、参数演示或技术选型说教（如“为什么不用XXX”）。
- **极简与克制**：自解释代码绝不写注释；仅在必要时阐述非显式的底层逻辑、Edge Case 或 Hack 的原因（Why），不逐行翻译执行过程（How）。
- **标准文档规范**：仅在文件头或关键导出 API 处保留精炼的 Docstring / JSDoc，客观描述功能、参数、返回值与异常，单条描述控制在 1~2 句内。

### 2. 运行时与日志输出规范 (Logging & Outputs)
- **严禁 Emoji 与拟人化**：控制台与日志中严禁使用任何 Emoji 表情（如 🚀、✅、❌、⚠️ 等），严禁使用客服式口吻（如“正在为您…”、“操作成功啦”）。
- **工业级格式化**：所有日志必须包含标准大写级别标签（`[DEBUG]`、`[INFO]`、`[WARN]`、`[ERROR]`），并附带模块/组件名称与关键键值信息。
  - 标准示例：`[INFO] [Server] Listening on 127.0.0.1:8686`
  - 标准示例：`[ERROR] [Storage] Write failed: disk full (path=/data/journal.log)`
- **客观陈述**：日志仅记录客观运行状态与状态跃迁，严禁混入用户指引或调试建议。

### 3. 应用内文案规范 (UI Microcopy & Messages)
- **严禁拟人、客套与感性表达**：
  - 严禁使用“亲”、“主人”、“您”、“哎呀”、“呀~”等拟人或客服式口吻。
  - 严禁使用感性修辞（如“开启美好记录”、“探索未知世界”）。
- **严禁滥用 Emoji**：按钮、菜单、Toast、弹窗及状态提示中严禁添加 Emoji（纯文本/系统级图标除外）。
- **动作导向与客观克制**：
  - 按钮与菜单统一使用简短动词或“动宾结构”（如：`保存`、`导出 Markdown`、`重置目录`，而非 `点我保存`、`帮我导出`）。
  - 空状态（Empty State）客观陈述当前事实及单步操作（如：`暂无笔记` / `新建笔记`，而非 `空空如也，快去写一篇吧~`）。
  - Toast 提示直截了当（如：`配置已保存`、`同步完成`、`复制成功`，而非 `保存成功啦！`、`已为您处理好`）。
- **错误与异常提示规范**：
  - 严禁模糊的“发生未知错误”。
  - 必须清晰陈述“失败动作 + 核心原因 + 可行解决路径”（如：`文件保存失败：磁盘空间不足，请清理空间后重试`）。

## 不污染仓库

### 任何用完即删，仅用来给你测试的东西，都需放在`.github/agent/tmp`文件夹下。

### 严禁更改`AGENTS.md`文件